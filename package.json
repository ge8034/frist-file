{
  "name": "guandan2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "pyright",
    "build:cloudflare": "echo \"🚀 开始 Cloudflare Pages 构建...\" && next build && echo \"✅ Next.js 构建完成\" && echo \"🧹 清理缓存目录...\" && rm -rf .next/cache && echo \"📁 检查构建输出...\" && (ls -la .next/ 2>/dev/null || dir .next) && echo \"📋 文件统计:\" && echo \"- .next/ 目录大小:\" && (du -sh .next 2>/dev/null || echo \"请手动检查\") && echo \"✅ Cloudflare Pages 构建完成\"",
    "build:cf-debug": "echo \"🔍 Cloudflare Pages 调试构建...\" && echo \"1. 清理旧构建...\" && rm -rf .next dist out && echo \"2. 构建 Next.js...\" && next build && echo \"3. 创建测试文件...\" && cp index.html .next/ 2>/dev/null || echo \"无法复制 index.html\" && cp public/test.html .next/ 2>/dev/null || echo \"无法复制 test.html\" && echo \"4. 显示构建结果...\" && find .next -name \"*.html\" -o -name \"*.js\" | head -20 && echo \"5. 创建 dist 目录备用...\" && mkdir -p dist && cp -r .next/* dist/ 2>/dev/null || echo \"复制到 dist 失败\" && echo \"✅ 调试构建完成\"",
    "build:cf-dist": "echo \"📦 构建到 dist 目录...\" && rm -rf dist && next build && mkdir -p dist && cp -r .next/* dist/ && rm -rf .next/cache && echo \"✅ 已构建到 dist 目录\"",
    "build:cf-static": "echo \"📄 尝试静态导出...\" && rm -rf out && echo \"1. 构建 Next.js...\" && next build && echo \"2. 执行静态导出...\" && next export 2>&1 || (echo \"⚠️  静态导出遇到问题，继续处理...\" && mkdir -p out && echo '<html><body><h1>导出失败，请检查动态路由</h1></body></html>' > out/index.html) && echo \"3. 检查输出...\" && (ls -la out/ 2>/dev/null || dir out) && echo \"✅ 静态导出完成\"",
    "build:cf-static-force": "echo \"🚀 强制静态导出（忽略动态路由错误）...\" && rm -rf out && next build && (next export --outdir out 2>&1 | grep -v 'Error\\|Warn' || true) && echo \"创建回退页面...\" && find out -name \"*.html\" | head -5 && echo \"✅ 强制静态导出完成\"",
    "build:cf-test": "echo \"🧪 简单测试构建...\" && echo '<!DOCTYPE html><html><head><title>Test</title></head><body><h1>Cloudflare Test</h1></body></html>' > test-simple.html && mkdir -p dist-test && cp test-simple.html dist-test/ && echo \"✅ 测试构建完成\"",
    "build:cf-html-only": "echo \"🔄 创建最简单的 HTML 测试...\" && rm -rf html-test && mkdir -p html-test && echo '<!DOCTYPE html><html><head><title>Cloudflare Pages Test</title><style>body{font-family:Arial;max-width:800px;margin:40px auto;padding:20px;}</style></head><body><h1>✅ Cloudflare Pages 工作正常！</h1><p>如果看到此页面，说明 Cloudflare Pages 配置正确。</p><p>时间: <span id=\"time\"></span></p><script>document.getElementById(\"time\").textContent=new Date().toLocaleString()</script></body></html>' > html-test/index.html && echo '/* /index.html 200' > html-test/_redirects && echo \"✅ HTML 测试构建完成\""
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "@supabase/auth-helpers-nextjs": "^0.10.0",
    "@supabase/realtime-js": "^2.15.0",
    "@supabase/supabase-js": "^2.45.4",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.563.0",
    "next": "14.2.18",
    "phaser": "^3.80.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwind-merge": "^3.4.0",
    "uuid": "^10.0.0",
    "zod": "^3.23.8",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/react": "^16.0.0",
    "@types/node": "^20.19.32",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/uuid": "^10.0.0",
    "@vitest/ui": "^4.0.18",
    "autoprefixer": "^10.4.19",
    "eslint": "^8.57.0",
    "eslint-config-next": "14.2.18",
    "jest": "^29.7.0",
    "postcss": "^8.4.40",
    "pyright": "^1.1.382",
    "tailwindcss": "^3.4.6",
    "ts-jest": "^29.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
